@model IEnumerable<Roll.atributo>

@{
    Layout = "/Views/personajes/_LayoutPage1.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.atributo_nombre)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.atributo_nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_atributo }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_atributo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_atributo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
