@model IEnumerable<Roll.estado>

@{
    Layout = "/Views/personajes/_LayoutPage1.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.est_nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.est_descrip)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.est_nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.est_descrip)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_estado }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_estado }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_estado })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
