@model IEnumerable<Roll.mochilas>

@{
    Layout = "/Views/personajes/_LayoutPage1.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.multiplicador_peso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.capacidad)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.multiplicador_peso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.capacidad)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id_mochila }) |
                @Html.ActionLink("Details", "Details", new { id=item.id_mochila }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id_mochila })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
